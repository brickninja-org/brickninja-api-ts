{
  "name": "brickninjaapi",
  "private": true,
  "scripts": {
    "test": "pnpm run -r test",
    "version": "changeset version && pnpm install --lockfile-only --link-workspace-packages",
    "release": "pnpm run -r build && changeset publish"
  },
  "engines": {
    "npm": "please-use-pnpm",
    "yarn": "please-use-pnpm",
    "pnpm": "11.8.0"
  },
  "dependencies": {
    "@changesets/cli": "2.31.0"
  },
  "devDependencies": {
    "@changeset-bot/changelog": "0.0.4",
    "typescript": "6.0.3"
  },
  "packageManager": "pnpm@11.8.0"
}
